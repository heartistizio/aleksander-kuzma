import React from 'react';

import './contact.scss';

const Contact = (props) => {
    return(
        <section id="contact">
            <div className="contact">
                <h1>Contact</h1>
            </div>
        </section>
    )
}

export default Contact;